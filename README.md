# Tip Prediction Regression
This project uses a Linear Regression model to predict tip amounts using the Seaborn tips dataset.

## How to Run
1. Ensure you have Python installed.
2. Install requirements: `pip install pandas numpy seaborn scikit-learn matplotlib`
3. Run the script: `python analysis.py`
4. View `visualisation.png` for the feature importance chart.